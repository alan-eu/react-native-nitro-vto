///
/// HybridGlassesVTOViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridGlassesVTOViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::glassesvto::views {

  extern const char HybridGlassesVTOViewComponentName[] = "GlassesVTOView";

  HybridGlassesVTOViewProps::HybridGlassesVTOViewProps(const react::PropsParserContext& context,
                                                       const HybridGlassesVTOViewProps& sourceProps,
                                                       const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    modelPath([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("modelPath", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.modelPath;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.modelPath);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("GlassesVTOView.modelPath: ") + exc.what());
      }
    }()),
    modelWidthMeters([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("modelWidthMeters", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.modelWidthMeters;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.modelWidthMeters);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("GlassesVTOView.modelWidthMeters: ") + exc.what());
      }
    }()),
    isActive([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("isActive", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.isActive;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.isActive);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("GlassesVTOView.isActive: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridGlassesVTOViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridGlassesVTOViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("GlassesVTOView.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridGlassesVTOViewProps::HybridGlassesVTOViewProps(const HybridGlassesVTOViewProps& other):
    react::ViewProps(),
    modelPath(other.modelPath),
    modelWidthMeters(other.modelWidthMeters),
    isActive(other.isActive),
    hybridRef(other.hybridRef) { }

  bool HybridGlassesVTOViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("modelPath"): return true;
      case hashString("modelWidthMeters"): return true;
      case hashString("isActive"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridGlassesVTOViewComponentDescriptor::HybridGlassesVTOViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridGlassesVTOViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                          const std::shared_ptr<const react::Props>& props,
                                                                                          react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridGlassesVTOViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridGlassesVTOViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridGlassesVTOViewShadowNode&>(shadowNode);
    const HybridGlassesVTOViewProps& props = concreteShadowNode.getConcreteProps();
    HybridGlassesVTOViewState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::glassesvto::views
