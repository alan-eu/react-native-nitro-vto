///
/// JHybridGlassesVTOViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "JHybridGlassesVTOViewStateUpdater.hpp"
#include "views/HybridGlassesVTOViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>

namespace margelo::nitro::glassesvto::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridGlassesVTOViewState>;

void JHybridGlassesVTOViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridGlassesVTOViewSpec::javaobject> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  JHybridGlassesVTOViewSpec* view = javaView->cthis();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};

  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridGlassesVTOViewState& data = concreteState->getData();
  const std::optional<HybridGlassesVTOViewProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridGlassesVTOViewState's data doesn't contain any props!");
  }
  const HybridGlassesVTOViewProps& props = maybeProps.value();
  if (props.modelPath.isDirty) {
    view->setModelPath(props.modelPath.value);
    // TODO: Set isDirty = false
  }
  if (props.modelWidthMeters.isDirty) {
    view->setModelWidthMeters(props.modelWidthMeters.value);
    // TODO: Set isDirty = false
  }
  if (props.isActive.isDirty) {
    view->setIsActive(props.isActive.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      std::shared_ptr<JHybridGlassesVTOViewSpec> shared = javaView->cthis()->shared_cast<JHybridGlassesVTOViewSpec>();
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::glassesvto::views
