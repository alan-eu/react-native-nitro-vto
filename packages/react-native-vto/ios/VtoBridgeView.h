#import <UIKit/UIKit.h>

/**
 * Forward declaration so the RCTViewManager (Objective-C++) can refer to
 * `VtoBridgeView` without needing to pull in the Swift-generated header at
 * every call site. The real class is defined in VtoBridgeView.swift.
 */
@class VtoBridgeView;
