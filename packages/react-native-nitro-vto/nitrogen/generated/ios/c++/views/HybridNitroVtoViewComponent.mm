///
/// HybridNitroVtoViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridNitroVtoViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridNitroVtoViewSpecSwift.hpp"
#import "NitroVto-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::nitrovto;
using namespace margelo::nitro::nitrovto::views;

/**
 * Represents the React Native View holder for the Nitro "NitroVtoView" HybridView.
 */
@interface HybridNitroVtoViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridNitroVtoViewComponent {
  std::shared_ptr<HybridNitroVtoViewSpecSwift> _hybridView;
  BOOL _didDropView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridNitroVtoViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridNitroVtoViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    _props = HybridNitroVtoViewShadowNode::defaultSharedProps();
    std::shared_ptr<HybridNitroVtoViewSpec> hybridView = NitroVto::NitroVtoAutolinking::createNitroVtoView();
    _hybridView = std::dynamic_pointer_cast<HybridNitroVtoViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroVto::HybridNitroVtoViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) notifyOnDropView {
  // A recycled component can later be invalidated. Notify only once per mount.
  if (_didDropView) {
    return;
  }
  NitroVto::HybridNitroVtoViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  _didDropView = YES;
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // A props update marks a newly mounted or still-active component.
  _didDropView = NO;

  // 1. Downcast props
  const auto& newViewProps = *std::static_pointer_cast<const HybridNitroVtoViewProps>(props);
  const auto* oldViewProps = static_cast<const HybridNitroVtoViewProps*>(oldProps.get());
  NitroVto::HybridNitroVtoViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update only props that differ from the previous Props snapshot.
  const bool hasTransactionPropChanges = oldViewProps == nullptr
      ? newViewProps.hasAnyProvidedProps()
      : !newViewProps.hasSameProps(*oldViewProps);
  if (hasTransactionPropChanges) {
    swiftPart.beforeUpdate();

    // modelUrl: string
    if (oldViewProps == nullptr
          ? newViewProps.modelUrl.isProvided()
          : !newViewProps.modelUrl.hasSameValue(oldViewProps->modelUrl)) {
      swiftPart.setModelUrl(newViewProps.modelUrl.get());
    }
    // isActive: boolean
    if (oldViewProps == nullptr
          ? newViewProps.isActive.isProvided()
          : !newViewProps.isActive.hasSameValue(oldViewProps->isActive)) {
      swiftPart.setIsActive(newViewProps.isActive.get());
    }
    // onModelLoaded: optional
    if (oldViewProps == nullptr
          ? newViewProps.onModelLoaded.isProvided()
          : !newViewProps.onModelLoaded.hasSameValue(oldViewProps->onModelLoaded)) {
      swiftPart.setOnModelLoaded(newViewProps.onModelLoaded.get());
    }
    // onFaceTracked: optional
    if (oldViewProps == nullptr
          ? newViewProps.onFaceTracked.isProvided()
          : !newViewProps.onFaceTracked.hasSameValue(oldViewProps->onFaceTracked)) {
      swiftPart.setOnFaceTracked(newViewProps.onFaceTracked.get());
    }
    // onArUnavailable: optional
    if (oldViewProps == nullptr
          ? newViewProps.onArUnavailable.isProvided()
          : !newViewProps.onArUnavailable.hasSameValue(oldViewProps->onArUnavailable)) {
      swiftPart.setOnArUnavailable(newViewProps.onArUnavailable.get());
    }
    // onGlassesDisplayed: optional
    if (oldViewProps == nullptr
          ? newViewProps.onGlassesDisplayed.isProvided()
          : !newViewProps.onGlassesDisplayed.hasSameValue(oldViewProps->onGlassesDisplayed)) {
      swiftPart.setOnGlassesDisplayed(newViewProps.onGlassesDisplayed.get());
    }
    // forwardOffset: optional
    if (oldViewProps == nullptr
          ? newViewProps.forwardOffset.isProvided()
          : !newViewProps.forwardOffset.hasSameValue(oldViewProps->forwardOffset)) {
      swiftPart.setForwardOffset(newViewProps.forwardOffset.get());
    }
    // debug: optional
    if (oldViewProps == nullptr
          ? newViewProps.debug.isProvided()
          : !newViewProps.debug.hasSameValue(oldViewProps->debug)) {
      swiftPart.setDebug(newViewProps.debug.get());
    }
    // showNativeFPS: optional
    if (oldViewProps == nullptr
          ? newViewProps.showNativeFPS.isProvided()
          : !newViewProps.showNativeFPS.hasSameValue(oldViewProps->showNativeFPS)) {
      swiftPart.setShowNativeFPS(newViewProps.showNativeFPS.get());
    }
    // mode: optional
    if (oldViewProps == nullptr
          ? newViewProps.mode.isProvided()
          : !newViewProps.mode.hasSameValue(oldViewProps->mode)) {
      swiftPart.setMode(newViewProps.mode.get());
    }
    // previewBackgroundColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.previewBackgroundColor.isProvided()
          : !newViewProps.previewBackgroundColor.hasSameValue(oldViewProps->previewBackgroundColor)) {
      swiftPart.setPreviewBackgroundColor(newViewProps.previewBackgroundColor.get());
    }
    // isClipOn: boolean
    if (oldViewProps == nullptr
          ? newViewProps.isClipOn.isProvided()
          : !newViewProps.isClipOn.hasSameValue(oldViewProps->isClipOn)) {
      swiftPart.setIsClipOn(newViewProps.isClipOn.get());
    }

    // Update hybridRef if it changed
    if (oldViewProps == nullptr
          ? newViewProps.hybridRef.isProvided()
          : !newViewProps.hybridRef.hasSameValue(oldViewProps->hybridRef)) {
      // hybridRef changed - call it with new this
      const auto& maybeFunc = newViewProps.hybridRef.get();
      if (maybeFunc.has_value()) {
        maybeFunc.value()(_hybridView);
      }
    }

    swiftPart.afterUpdate();
  }

  // 3. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroVto::NitroVtoAutolinking::isNitroVtoViewRecyclable();
}

- (void)prepareForRecycle {
  [self notifyOnDropView];
  [super prepareForRecycle];
  NitroVto::HybridNitroVtoViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  [self notifyOnDropView];
  [super invalidate];
}
#endif

@end
