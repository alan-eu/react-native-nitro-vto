///
/// JHybridNitroVtoViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridNitroVtoViewStateUpdater.hpp"
#include "views/HybridNitroVtoViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::nitrovto::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridNitroVtoViewState>;

std::shared_ptr<const HybridNitroVtoViewProps> JHybridNitroVtoViewStateUpdater::getPropsFromStateWrapper(
    jni::alias_ref<JStateWrapper::javaobject> stateWrapper) {
  if (stateWrapper.get() == nullptr) {
    return nullptr;
  }
  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapper.get();
  if (!stateWrapper->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
    throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapperImpl = jni::alias_ref<react::StateWrapperImpl::javaobject>{
    static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)
  };
  std::shared_ptr<const react::State> state = stateWrapperImpl->cthis()->getState();
  if (state == nullptr) {
    return nullptr;
  }
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridNitroVtoViewState& data = concreteState->getData();
  const std::shared_ptr<const HybridNitroVtoViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    throw std::runtime_error("HybridNitroVtoViewState's data doesn't contain any props!");
  }
  return props;
}

void JHybridNitroVtoViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridNitroVtoViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> newState,
                                           jni::alias_ref<JStateWrapper::javaobject> oldState) {
  std::shared_ptr<JHybridNitroVtoViewSpec> hybridView = javaView->getJHybridNitroVtoViewSpec();
  std::shared_ptr<const HybridNitroVtoViewProps> newProps = getPropsFromStateWrapper(newState);
  std::shared_ptr<const HybridNitroVtoViewProps> oldProps = getPropsFromStateWrapper(oldState);
  if (newProps == nullptr) [[unlikely]] {
    throw std::runtime_error("Current StateWrapper doesn't contain any props!");
  }

  // Update only props that differ from the previous State snapshot.
  if (oldProps == nullptr
        ? newProps->modelUrl.isProvided()
        : !newProps->modelUrl.hasSameValue(oldProps->modelUrl)) {
    hybridView->setModelUrl(newProps->modelUrl.get());
  }
  if (oldProps == nullptr
        ? newProps->isActive.isProvided()
        : !newProps->isActive.hasSameValue(oldProps->isActive)) {
    hybridView->setIsActive(newProps->isActive.get());
  }
  if (oldProps == nullptr
        ? newProps->onModelLoaded.isProvided()
        : !newProps->onModelLoaded.hasSameValue(oldProps->onModelLoaded)) {
    hybridView->setOnModelLoaded(newProps->onModelLoaded.get());
  }
  if (oldProps == nullptr
        ? newProps->onFaceTracked.isProvided()
        : !newProps->onFaceTracked.hasSameValue(oldProps->onFaceTracked)) {
    hybridView->setOnFaceTracked(newProps->onFaceTracked.get());
  }
  if (oldProps == nullptr
        ? newProps->onArUnavailable.isProvided()
        : !newProps->onArUnavailable.hasSameValue(oldProps->onArUnavailable)) {
    hybridView->setOnArUnavailable(newProps->onArUnavailable.get());
  }
  if (oldProps == nullptr
        ? newProps->onGlassesDisplayed.isProvided()
        : !newProps->onGlassesDisplayed.hasSameValue(oldProps->onGlassesDisplayed)) {
    hybridView->setOnGlassesDisplayed(newProps->onGlassesDisplayed.get());
  }
  if (oldProps == nullptr
        ? newProps->forwardOffset.isProvided()
        : !newProps->forwardOffset.hasSameValue(oldProps->forwardOffset)) {
    hybridView->setForwardOffset(newProps->forwardOffset.get());
  }
  if (oldProps == nullptr
        ? newProps->debug.isProvided()
        : !newProps->debug.hasSameValue(oldProps->debug)) {
    hybridView->setDebug(newProps->debug.get());
  }
  if (oldProps == nullptr
        ? newProps->showNativeFPS.isProvided()
        : !newProps->showNativeFPS.hasSameValue(oldProps->showNativeFPS)) {
    hybridView->setShowNativeFPS(newProps->showNativeFPS.get());
  }
  if (oldProps == nullptr
        ? newProps->mode.isProvided()
        : !newProps->mode.hasSameValue(oldProps->mode)) {
    hybridView->setMode(newProps->mode.get());
  }
  if (oldProps == nullptr
        ? newProps->previewBackgroundColor.isProvided()
        : !newProps->previewBackgroundColor.hasSameValue(oldProps->previewBackgroundColor)) {
    hybridView->setPreviewBackgroundColor(newProps->previewBackgroundColor.get());
  }
  if (oldProps == nullptr
        ? newProps->isClipOn.isProvided()
        : !newProps->isClipOn.hasSameValue(oldProps->isClipOn)) {
    hybridView->setIsClipOn(newProps->isClipOn.get());
  }

  // Update hybridRef if it changed
  if (oldProps == nullptr
        ? newProps->hybridRef.isProvided()
        : !newProps->hybridRef.hasSameValue(oldProps->hybridRef)) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newProps->hybridRef.get();
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
  }
}

} // namespace margelo::nitro::nitrovto::views
